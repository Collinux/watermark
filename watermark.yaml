watermark:
        # margins around the image in its placement (see next block)
        padding_left: 0
        padding_right: 0
        padding_top: 0
        padding_bottom: 0

        # center, top_left, top_right, bottom_left, bottom_right
        position: bottom_right

        # file name of watermark png
        source: watermark.png
